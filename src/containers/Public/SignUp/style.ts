import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: {
		padding: 0,
	},

	scrollView: {
		alignItems: 'center',
		paddingBottom: 20,
	},

});

export default styles;